# -*- coding: utf-8 -*-
"""
「新版介面速覽」對照表（v1.52.1 第二輪）。

v1.52 把主視窗從「11 顆工具列按鈕 + 一整頁欄位」改成四個階段頁籤，
`docs/UI_ARCHITECTURE_2.0.md` D 節點名的風險就是老用戶的肌肉記憶會落
空——按鈕沒有消失，只是搬家了，但使用者第一眼看到的是「不見了」。

所以這裡不做那種「歡迎使用新版！」的行銷頁，而是一張**舊位置 → 新位置**
的對照表：使用者心裡想的是「我的某某功能呢」，這張表就照那個問法回答。
兩顆被改名的按鈕也列進來（D-1），因為改名比搬家更難靠找的找到。

首次啟動時由 `gui/app.py` 呼叫；勾「不再顯示」後寫入 config 的
`whatsnew_seen`，之後任何版本都不再跳（見 `NEVER_SHOW`）。

**2.0 補上「新增了什麼」那一半**：v1.52.1 這裡只講搬家，因為那一版就只是
搬家。但依發佈政策，v1.51.0 之後每一版都是測試版、不推自動更新，使用者是
**從 1.51.0 一次跳到 2.0.0** 的——中間四版的新能力他一樣都沒看過。只講搬
家會讓人以為 2.0 只是換了個排版。
"""

import tkinter as tk
from tkinter import ttk

# 使用者勾了「不再顯示」時寫進 config["whatsnew_seen"] 的值。用哨符字串
# 而不是布林，是為了和「看過 1.52.1 這一版」區分開來：前者是永久關閉，
# 後者只代表這一版看過了，日後又一次大改介面仍可再導覽一次。
NEVER_SHOW = "never"

# (舊位置, 新位置) 對照。順序照使用者最可能先找哪一個排，不是照程式碼順序。
MOVED = [
    ("工具列〔審片助手（找片段）〕", "① 素材與剪輯"),
    ("工具列〔字幕健檢〕〔章節健檢〕〔發佈健檢〕〔封面健檢〕等健檢類",
     "③ 健檢中心（v2.2 起就是那個頁籤本身，連窗都不用開）"),
    ("工具列〔配樂助手〕〔品牌套版〕", "④ 輸出與發佈 → 成品加工"),
    ("清單編輯列〔匯入字幕〕", "① 素材與剪輯（匯入是來源，不是清單操作）"),
    ("「匯出與燒錄」區", "④ 輸出與發佈 →「輸出」框下半"),
    ("「自動化輸出（一鍵完成用）」區", "④ 輸出與發佈 →「輸出」框中段，已改名「輸出設定」"),
]

# (舊名, 新名, 為什麼改) 對照。
RENAMED = [
    ("自動跳剪停頓", "剪停頓（依字幕）", "原名看不出它吃的是字幕還是影片"),
    ("重複片段偵測", "剪重複片段", "原名只說偵測，其實會剪"),
]

# 2.0：新增與改善的能力。v1.52.1 這個對話框只講「搬到哪裡了」，因為那一
# 版就只是搬家。但依發佈政策，v1.51.0 之後的每一版都是測試版、不推自動更
# 新，所以使用者是**從 1.51.0 一次跳到 2.0.0** 的——中間四版的新東西他一
# 樣都沒看過。只講搬家會讓人以為 2.0 只是換了個排版。
NEW_FEATURES = [
    # v2.3：排最前面——從 v2.1.0 更新上來的人，這是他這次最大的新東西。
    ("螢幕畫面翻譯（v2.3）",
     "〔即時查譯〕查譯頁最上面的〔框選螢幕翻譯〕：拖出一塊畫面，在這台電"
     "腦上把字認出來，原文可以改，確認後按〔翻譯辨識結果〕才送。遊戲、影"
     "片裡燒死的字幕、圖片裡的字都能翻；英、日、中文自動挑。畫面不會被傳"
     "出去，像密碼的內容不會送出。"),
    ("朗讀、複製與熱鍵 Ctrl+Alt+F8（v2.3）",
     "原文與譯文都可以朗讀、複製，不需要 API 金鑰。勾〔熱鍵 Ctrl+Alt+F8〕"
     "後，在任何程式裡按就直接開始框選（預設關閉）。Windows 上的截圖、熱"
     "鍵與朗讀還沒有在實機上確認過，遇到問題請回報。"),
    ("健檢中心就是階段③頁籤本身（v2.2）",
     "點一下〔③ 健檢中心〕，檢查對象、要跑哪些檢查、分級報告、逐項修復"
     "都在同一個畫面上，不必再開一個視窗、用完再關。18 項勾選左右並排，"
     "預設視窗尺寸下一次全看得到；封面圖與系列影片預設收合，要用再展開。"),
    ("健檢看的是你現在的字幕，不是開窗當下的那一份（v2.2）",
     "在階段②改完錯字切回階段③重跑，報告講的就是新句子。以前得把健檢"
     "視窗關掉重開才會更新。"),
    ("做出來的東西會自動流到下一步",
     "粗剪、修復版、燒錄版做完會問你要不要設為「目前影片」，接手後直接"
     "接著用；審片產生的章節、發佈包與封面候選自動流進階段④「發佈資"
     "料」，一顆〔送健檢中心〕就送去檢查。以前這些都只跳一個訊息框報路"
     "徑，要自己記住路徑回主視窗重選檔案。"),
    ("工作檔案列會顯示這支影片是怎麼來的",
     "例如「原始素材 訪談.mp4 → 粗剪 → 修復版（目前）」。"),
    ("輸出區會先告訴你這次會產出什麼",
     "階段④「輸出」框最上面那句話會寫出「匯出 SRT＋燒錄硬字幕影片 → "
     "D:\\成品」，勾選一改立刻跟著變，按之前就知道結果。"),
    ("健檢中心：六個健檢視窗併成一個",
     "影片、字幕、封面、發佈文字全部選填、填什麼檢什麼，18 項檢查一次跑"
     "完，9 個發現旁邊就是一鍵修復。"),
    ("即時查譯",
     "選取逐字稿或字幕裡的一段外文，鬆開滑鼠就翻成中文，附關鍵詞解說，"
     "生字本可匯出 CSV。需要 OpenAI API 金鑰。"),
    ("深色主題與版面修好了",
     "數字輸入框在深色下不再是刺眼的白框；字幕清單的時間欄放得下完整時"
     "間碼；視窗縮小時右欄自動收合，主動作與字幕清單永遠可見。"),
]

# 實測的省力幅度。數字是當場量的，不是估的——量法與 docs/UI_AUDIT_2.0.md
# 1.2 節同一個情境、同一套計數規則。
SAVINGS_TEXT = (
    "同一個情境實走（一支剪好的影片 → 燒好字幕的成品＋SRT＋章節/發佈資"
    "訊/封面都檢過）：\n"
    "　點擊　　43–46 次 → 30 次\n"
    "　開啟視窗　17–19 個 → 6 個\n"
    "　主按鈕　y=826（畫面外，要捲動找）→ y=131（一開啟就看得到）\n\n"
    "剩下的 30 次裡有 17 次是校對本身——逐句修字是內容工作，介面重構省"
    "不到它。（2.0 時是 31 次 7 個視窗；v2.2 把健檢中心內嵌進頁籤之後"
    "又省下一擊一窗。）"
)

# 拆掉的按鈕要單獨講，因為它不是搬家也不是改名，是行為變了。
SPLIT_TEXT = (
    "〔一鍵完成（生成＋匯出＋燒錄）〕拆成並排的兩顆："
    "〔開始生成字幕〕與〔完成輸出（依輸出設定）〕。\n"
    "舊按鈕會一路衝到底，等於沒有校對的機會就把錯字燒進影片；"
    "拆開後中間那段空白就是校對、健檢、翻譯的位置。"
    "不需要校對的話，連按兩顆，結果和以前完全一樣。\n"
    "一次處理多個檔案時，動作列會自動多出〔批次一鍵完成〕，"
    "行為與舊按鈕一致（批次本來就不會逐支校對）。"
)


class WhatsNewDialog(tk.Toplevel):
    """新版介面速覽；關閉時把「不再顯示」的選擇回報給 on_close。"""

    def __init__(self, master, version, on_close=None):
        super().__init__(master)
        self.title("新版介面速覽")
        self.transient(master)
        self.resizable(True, True)
        self.geometry("720x620")
        self.minsize(600, 480)
        self._on_close = on_close

        body = ttk.Frame(self, padding=14)
        body.pack(fill="both", expand=True)

        ttk.Label(
            body, justify="left", wraplength=660,
            text=f"這一版（{version}）的主視窗是四個階段頁籤：\n"
                 "① 素材與剪輯 → ② 字幕 → ③ 健檢中心 → ④ 輸出與發佈。\n"
                 "頁籤的順序就是做一支影片的順序，原本的功能一個都沒有拿掉，"
                 "只是搬到它該在的階段。\n"
                 # 明寫換行：Tk 在「v2.1.0」後面那個空白斷行，後面整串中
                 # 文擠到下一行，讀起來像斷了一截（截圖才看到）。
                 "如果你是從 v2.1.0 或更早的版本更新上來的，\n"
                 "中間幾版的新東西也會一次全部到齊——下面兩段都值得看一下。",
        ).pack(anchor="w", pady=(0, 10))

        canvas_holder = ttk.Frame(body)
        canvas_holder.pack(fill="both", expand=True)
        # 捲軸先 pack（side="right"）再 pack 內容區，順序反了會讓捲軸被
        # 擠成 1px——v1.48.0 出過這個包，見 docs/ROADMAP_2.0.md 紀律節。
        scrollbar = ttk.Scrollbar(canvas_holder, orient="vertical")
        scrollbar.pack(side="right", fill="y")
        text = tk.Text(
            canvas_holder, wrap="word", height=10, relief="flat",
            yscrollcommand=scrollbar.set, padx=8, pady=6,
        )
        text.pack(side="left", fill="both", expand=True)
        scrollbar.configure(command=text.yview)
        text.insert("end", self._build_body())
        text.configure(state="disabled")

        self.never_var = tk.BooleanVar(value=False)
        ttk.Checkbutton(
            body, text="不再顯示這個速覽", variable=self.never_var,
        ).pack(anchor="w", pady=(10, 0))

        ttk.Button(body, text="開始使用", width=12, command=self._close).pack(
            anchor="e", pady=(8, 0))
        self.protocol("WM_DELETE_WINDOW", self._close)

    def _build_body(self):
        """組出對照表全文。純字串，方便測試直接驗內容。"""
        # 順序是有意的：**先講「你多了什麼」與「哪裡不一樣了」，搬家對照
        # 表排到後面**。從 v1.51.0 跳上來的人第一眼想知道的是這一版給了他
        # 什麼；「某某功能搬到哪」是他日後找不到東西時才會回來查的表。
        # 1.52.1 版把搬家表放第一是對的（那一版就只是搬家），2.0 不是。
        lines = ["【新增與改善的能力】", ""]
        for title, detail in NEW_FEATURES:
            lines.append(f"　◆ {title}")
            lines.append(f"　　{detail}")
            lines.append("")
        lines += ["【「一鍵完成」拆成兩顆】", "", SPLIT_TEXT, ""]
        lines += ["【這次省了多少（實測）】", "", SAVINGS_TEXT, ""]
        lines += ["【功能搬到哪裡了】", ""]
        for old, new in MOVED:
            lines.append(f"　{old}\n　　→ {new}")
        lines += ["", "【改名的按鈕】", ""]
        for old, new, why in RENAMED:
            lines.append(f"　{old} → 「{new}」（{why}）")
        return "\n".join(lines)

    def _close(self):
        if self._on_close:
            self._on_close(bool(self.never_var.get()))
        self.destroy()


def should_show(config, version):
    """
    是否該在啟動時跳出速覽。

    看過這一版、或勾過「不再顯示」就不跳。抽成函式讓 GUI 那端只剩一行
    判斷，也讓這個規則可以不開視窗就測。
    """
    seen = (config.get("whatsnew_seen") or "").strip()
    return seen != NEVER_SHOW and seen != version
