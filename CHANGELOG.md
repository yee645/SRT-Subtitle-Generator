# 版本更新紀錄

## v1.36.0（2026-08-09）

主題：**發佈資訊健檢**——hashtag 超量會整批失效，而 YouTube 不會告訴你。

### 發佈資訊健檢（標題／說明欄／hashtag／標籤）
- 調研引用（中英文皆搜）確認的規則全是可機器檢查的硬性數字：標題 100 字元、說明欄 5,000 位元組、hashtag 最多 15 個、標籤欄位 500 字元預算
- **最重要的一項：hashtag 超過 15 個時，YouTube 會「忽略全部」而且不給任何提示**。不是超出的那幾個失效，是整批都不生效——創作者堆了 16 個想要更多曝光，結果得到零。這與 v1.32.0 的章節不顯示是同一種靜默失敗模式
- **對中文使用者的關鍵差異：說明欄的上限算的是「位元組」不是字數**。中文一個字佔 3 個位元組，所以中文說明大約寫到 1,600 多字就會撞上限。實測 **1,690 個中文字 ＝ 5,070 位元組**，用 `len()` 檢查會判定「沒超過」，實際上早就被擋下。本模組因此一律以 UTF-8 位元組計算
- 本工具自 v1.10.0 起就會產生發佈包（標題候選＋描述草稿＋標籤清單），但**從未檢查過任何一項上限**——實測產出的發佈包裡沒有出現任何上限提醒
- 新增 `subtitle/publishcheck.py`（零 GUI 依賴），吃**使用者直接貼上的文字**，不限於本工具的產出——最常見的情境正是「我自己寫的說明欄，為什麼 hashtag 沒有作用」
- 另外處理兩個容易漏掉的細節：**重複的 hashtag 只算一次卻仍佔用 15 個額度**；**標題裡的 hashtag 同樣佔用額度**，兩者都會一併計算
- 標籤字元預算把**分隔用的逗號也算進去**（YouTube 的標籤欄位就是一整串逗號分隔的文字），否則會低估而放行實際上超量的輸入
- 工具列新增「發佈健檢」按鈕；CLI `--publishcheck 發佈資訊檔`（第一行為標題、其餘為說明欄）與 `--tags`
- 各項上限皆可調並自動記憶（`config.json` 的 `publishcheck`）
- 只報告不自動改：標題怎麼寫、要留哪幾個 hashtag 是創作決定——與 v1.26.0 以來的保守設計一致

### 修正
- 說明欄那條建議原本用了 markdown 粗體（`**位元組**`），而 `tk.Text` 會原樣顯示星號。原先是在報告格式化時把星號濾掉，改為從來源字串就不使用 markdown，避免留下需要下游清理的輸出

## v1.35.0（2026-08-08）

主題：**封面健檢**——這張縮圖在手機上還看得清楚嗎。

### 封面健檢（手機尺寸可讀性）
- 調研引用（中英文皆搜）：**YouTube 七成以上的觀看來自手機**，而手機清單裡的縮圖只有大約 **200×110 像素**。業界的自我檢查法就是「把封面縮到 10% 大小再看一眼，如果看不清就是失敗」——資料指出**近四成的封面失敗原因單純就是「縮小後看不清楚」**，畫面太雜、對比太低、灰濛濛不顯眼是最常見的三種死法。中文資料另補上官方建議的 16:9 比例與「標題一行不超過 10 個字」
- 本工具自 v1.11.0 起就會自動擷取封面候選圖，但**從未評估過產出的封面本身好不好用**——`thumbnails.py` 只在「挑哪一格」時算清晰度，挑完就結束。而最常見的情境其實是「我用 Canva／Photoshop 做好封面，這張能不能用」，那根本不是從影片幀來的
- 新增 `subtitle/thumbcheck.py`（零 GUI 依賴），吃**任意圖片檔**：
  - **手機尺寸可讀性**：把圖縮到 200 像素寬再用最近鄰放大回原尺寸，比較前後的邊緣能量。實測大色塊封面幾乎不掉，細碎雜訊型封面會崩到 **0.07**——這就是「縮小後糊成一團」的量化版本
  - **對比**：手機尺寸下的 YHIGH−YLOW。實測灰濛濛的封面只有 11，大色塊封面有 141
  - **色彩鮮明度**：平均飽和度
  - **規格**：解析度、16:9 比例、2MB 檔案大小上限（超過 YouTube 直接拒收）
- 一次可加入多張候選圖，依綜合分數排序並直接指出**建議使用哪一張**——正好接上 v1.11.0 產生的那批候選圖，補完「產生候選 → 該用哪一張」的迴路
- 工具列新增「封面健檢」按鈕（v1.34.0 的自動換行按鈕列讓第八個按鈕不必再調整視窗寬度，實測按鈕區所需寬度仍是 688px）；CLI `--thumbcheck`（圖片直接當位置參數傳入）
- 各項門檻皆可調並自動記憶（`config.json` 的 `thumbcheck`）
- 只報告不自動修：封面要怎麼改是設計決定，自動加濾鏡只會讓它更糟——與 v1.26.0 以來的保守設計一致
- 分數只用於同一批候選圖之間的相對比較，報告中明確註明不代表絕對品質

### 修正
- **細節保留率會顯示成「146%」**：比值可能大於 1（縮小後用最近鄰放大會造出原本沒有的硬邊），但「保留率超過 100%」對使用者沒有意義。改為夾在 1.0 以內
- **通過時的項目標題讀不通**：原本不論通過與否都叫「手機上看不清楚」，通過時會顯示成「✔ 手機上看不清楚」。所有項目標題改為中性名稱（手機尺寸可讀性／對比／色彩鮮明度／解析度／長寬比／檔案大小），通過與不通過共用同一個名稱
- `--thumbcheck` 原本設計成需要額外傳一個媒體檔佔位，實際上封面健檢與影片無關；改為圖片直接當位置參數傳入

## v1.34.0（2026-08-07）

主題：**剪輯節奏健檢**——抓出「畫面太久沒有變化」的段落。

### 剪輯節奏健檢（畫面變化密度）
- 調研引用（中英文皆搜）：**長時間維持單一畫面是留存殺手**。英文資料稱之為「cognitive underload」——畫面沒有新東西可看可處理，觀眾就走了；並給出可量化的基準：talking head 的常態節奏約**每 15~25 秒一次畫面變化**，每隔 2~3 分鐘再安排一小段密集快切。中文資料的說法是「都是維持靜態的片段會讓整個影片的可看性降低很多」「節奏忽快忽慢」
- **這與 v1.26.0 的凍結畫面偵測是相反的性質**：凍結看的是「畫面完全靜止」（錄製瑕疵、SD 卡卡頓），而 talking head 長鏡頭裡人是會動的——正好是它抓不到的情況。實測一段「前 30 秒完全沒有剪接」的素材，既有的凍結偵測、畫質健檢、去頭尾對那 30 秒完全沒有任何提示
- 新增 `subtitle/pacing.py`（零 GUI 依賴）：用 ffmpeg 的 scene 分數找出畫面變化點，把影片切成一個個「鏡頭」，檢查**畫面變化次數與平均鏡頭長度**、**超過上限沒有變化的段落**、**節奏是否忽快忽慢**（最長鏡頭遠超中位數），並直接給出**建議插入 B-roll／推鏡／圖卡的時間點**
- 整支影片一鏡到底時直接標為不合格；有剪接但個別鏡頭過長時逐段列出起訖時間
- 「上片前健檢」的「開始健檢」一併跑（同為 ffmpeg 分析，與畫質、色彩、音量一致性共用同一個報告）
- CLI `--pacecheck`（免轉錄），輸出「檔名_剪輯節奏.txt」
- 畫面不變上限與剪接偵測靈敏度可調並自動記憶（`config.json` 的 `pacing`）
- 只報告不自動改：要插 B-roll、推近鏡頭還是加圖卡是創作決定，而且工具手上也沒有使用者的 B-roll 素材——與 v1.26.0／v1.28.0／v1.29.0／v1.31.0／v1.32.0／v1.33.0 的保守設計一致

### 效能
- 掃描加上 `-an` 完全不解碼音訊（本分析只看畫面）：720p30／60 秒素材實測 CPU 從 4.6 秒降到 3.2 秒
- **原本打算「先縮圖再算 scene 分數」以省運算，但實測結果與直覺相反因而放棄**：scale 濾鏡自己每一格都要重採樣，成本高過它替 scene 偵測省下的量——同一支素材加了 `scale=320` 反而從 1.26 秒變成 3.43 秒，而偵測結果完全相同。相關的 `analysis_width` 設定項因此沒有保留，以免留下一個「調了只會更慢」的旋鈕

### 修正
- 一鏡到底的影片會同時報「整支沒有任何畫面變化」與「1 段畫面超過上限沒有變化」兩條重複訊息；改為前者成立時不再重複報後者（但 B-roll 建議插入點仍照常算出）
- `suggestions`（B-roll 建議插入點）原本只在 `analyze_pacing` 裡補上，直接呼叫 `evaluate_pacing` 拿到的結果會少這個欄位、報告也就少一段；改為由 `evaluate_pacing` 一併算出，兩條路徑的結果一致

### 維運
- v1.33.0 的 `release.yml` 至今仍未觸發（詳見該版維運備註），因此 v1.33.0 與本版都尚未產生 Release

## v1.33.0（2026-08-06）

主題：**開場健檢**——開頭十幾秒決定觀眾走不走，這裡檢查開頭到底在講什麼。

### 開場健檢（多久才進正題）
- 調研引用（中英日文皆搜，日文資料最完整）：**開頭十幾秒決定觀眾要不要看下去**，而流失主因幾乎都是同一組東西——冗長的打招呼、自我介紹、頻道宣傳、開場就先要訂閱，以及跟主題無關的閒聊。業界建議是「15 秒內要讓觀眾知道這支影片能給他什麼」，並且**把訂閱提醒移到影片後段**：對還沒被說服的新觀眾來說，一開場就要訂閱只會提前趕走他。另有資料明確建議刪掉每支影片重複使用的 5~10 秒開場動畫，因為訂閱者每次都會跳過
- 本工具已有 46 項功能，**沒有任何一項在看開頭講了什麼**：「一鍵去頭尾」偵測的是**靜音／黑畫面**的廢秒，講滿話的開場廢話抓不到；「廣告友善度」只在開頭區間找**粗話**；「字幕健檢」只看閱讀速度與行數。實測一段 22 秒全是打招呼＋自我介紹＋訂閱提醒的開場，這三項健檢全部沒有任何提示
- 新增 `subtitle/hookcheck.py`（零 GUI 依賴）：檢查**進正題時間**、**開場要訂閱**、**開場寒暄過長**、**無關閒聊／廢話轉場／贊助商／頻道雜務**、**開頭沒聲音**，並給出**建議的開場起點**
- **關鍵設計：判斷依據是「扣掉開場套語後這句還剩多少實質內容」，而不是單純比對關鍵詞**。「廢話不多說，今天要教大家怎麼用三個步驟做出專業影片」含有開場套語，但它同時就是正題本身；若只做關鍵詞比對，這一句會被誤判成廢話而把進正題時間一路往後推
- **訂閱提醒只有出現在正題之前才會被標記**——研究建議的正是「移到後段」，所以放在正題之後的訂閱提醒不該被罵
- 字幕健檢對話框的「開始健檢」一併跑開場健檢（同為純文字分析，不需要 ffmpeg，與字幕品質、廣告友善度共用同一次掃描與同一份報告）
- CLI `--hookcheck`，輸出「檔名_開場健檢.txt」，可與一般轉錄／對齊模式或 `--subs` 併用
- 進正題目標秒數、寒暄上限、開頭乾等上限皆可調並自動記憶（`config.json` 的 `hookcheck`）；另可用 `extra_filler_terms` 補充自己的口頭開場語、`ignore_terms` 排除頻道固定用語的誤判
- 只報告不自動剪：要不要保留自我介紹、開場要怎麼改寫是創作判斷——與 v1.26.0／v1.28.0／v1.29.0／v1.31.0／v1.32.0 的保守設計一致

### 修正
- **中英文的「實質內容」不能用同一種方式計算**：原本直接數扣掉虛字後的字元數，對英文而言任何一句都會因為字母多而遠超門檻，導致「so before we get started, don't forget to subscribe」這種純套語被判成有實質內容。改為中文按字數、英文按實詞數（一個實詞約當兩個中文字），兩種語言才落在同一個門檻上
- 補上「在開始之前」「幫我按」等常見開場套語——原本「在開始之前先幫我按個訂閱跟小鈴鐺」扣掉套語後仍殘留 6 個字，剛好卡在門檻上而被判成有實質內容
- **字幕健檢對話框新增第三組設定區後，下方報告區會被擠到只剩幾行**（三組設定區合計約 400px，但視窗高度仍是 740px、最小高度 620px）。視窗預設尺寸調整為 720x860、最小尺寸 660x740

### 維運備註
- 本版合併進 main 時 `release.yml` 未如常自動觸發（該次推送沒有產生任何 workflow run，當日 Actions 執行數為 0；儲存庫為公開、Actions 未停用，排除額度與設定問題），推測為 GitHub 端的推送事件未送達。已改以再次推送觸發路徑（`CHANGELOG.md`）的方式重新發版。
- 目前 `release.yml` 的自動觸發僅依賴 push 事件，事件漏送時沒有自動補救途徑；若日後再次發生，可由維護者在 Actions 頁面以 `workflow_dispatch` 手動補發。

## v1.32.0（2026-08-05）

主題：**YouTube 章節健檢**——章節貼上去卻不顯示、又沒有任何錯誤訊息時，直接告訴你原因。

### YouTube 章節健檢與一鍵修正
- 調研引用（中英文皆搜，英文社群資料較完整）：「章節貼進說明欄卻整批都不出現」是反覆出現的求助情境，難處在於 **YouTube 不會給任何錯誤訊息**——它只是靜靜地不顯示，創作者只能一條一條試錯猜哪裡錯了。而規則其實是明確且可機器檢查的：**首章必須是 0:00**（否則章節功能完全不啟用）、**至少 3 章**、**每章至少 10 秒**（只要任何一章太短，整批都不顯示），加上時間戳格式（半形冒號、秒數補滿兩位、時間戳與標題之間要有空白）
- 本工具自 v1.3.0 起就會產生 YouTube 章節草稿，但**從未檢查產出是否符合這些規則**。實測確認這是真的會踩到的洞：段落間隔不足的素材上，`build_chapters()` 只保證首章 0:00，會靜靜地只吐出 **1 章**，使用者複製貼上後章節完全不顯示，而工具全程沒有任何提示
- 新增「**章節健檢**」工具（工具列按鈕＋CLI `--chaptercheck`）：把上述規則變成一份逐條報告，指出是哪一行、哪一條規則出問題
- **輸入不限於本工具產生的章節**——手寫的、別的工具給的、從說明欄複製回來的都可以直接貼上檢查。這才是實務上最常見的求助情境，因此解析器對常見手寫錯誤**具體指認**（「時間戳要用半形冒號，不能用分號或句點」「秒數要補滿兩位，例如 0:05 而不是 0:5」「時間戳與標題之間要有一個空白」），而不是只回一句「格式錯誤」
- 「**一鍵修正**」在安全範圍內改好：修正打錯的格式、排序、移除重複時間、首章補成 0:00、把過短的章節併入前一章（保留前一章的標題，因為前一章才是該段落的主題）。每一項改動都列在報告裡，修正結果也直接寫回輸入框供再手動微調
- **不會憑空捏造章節**：段落本來就不足 3 章時如實回報「這不是格式問題、無法自動修正」，並指出可調整的方向——與 v1.26.0／v1.28.0／v1.29.0／v1.31.0 的保守設計一致
- 審片助手的「**複製 YouTube 章節**」接上同一套檢查：草稿不符規則時先擋下來說明原因，並詢問要不要改複製修正後的版本；連格式修正都救不回來（例如段落數本來就不夠）時明確說明無法自動處理。以往這裡會直接把貼上去不會顯示的草稿交給使用者
- 每章最短秒數與最少章節數可調並自動記憶（`config.json` 的 `chaptercheck`），預設值即 YouTube 目前的規則；做成可調是為了因應規則變動或其他平台
- CLI `--chaptercheck 章節檔`（輸出「檔名_章節健檢.txt」）與 `--chapterfix`（另存可直接貼回說明欄的「檔名_章節修正.txt」，原始檔不受影響）；影片檔用來取得長度，才能檢查最後一章

### 修正
- **一鍵修正會把格式打錯的行整行丟掉，報告卻顯示「全部合格」**——這是最容易誤導人的失敗方式：使用者以為修好了，實際上章節少了幾條。實測貼上含兩個格式錯誤的章節時，修正結果確實少了那兩章。改為先把意圖明確的錯誤（分隔符打錯、秒數少一位、時間戳與標題黏在一起）救回來，並在報告中逐條列出修改前後
- **拆解「時間戳與標題黏在一起」的樣式時，會把只有時間戳沒有標題的一行（例如 `1:00`）硬拆成時間戳 `1:0` 加標題 `0`，憑空生出一章**。改為先判斷整行本身是否就是一個完整時間戳，是的話直接視為無法自動修正
- **主視窗工具列加到第七個按鈕後會被視窗寬度切掉**（一列需要 1108px，最小視窗寬度為 980px）。功能按鈕改為每列最多四個、滿了自動換行，所需寬度降到 688px；日後再增加按鈕也不必跟著調大視窗

## v1.31.0（2026-08-04）

主題：**系列一致性檢查**——比對同一批影片「彼此之間」是否一致，而不是各自合不合格。

### 系列一致性檢查（跨檔比對）
- 調研引用（英文社群為主）：批次製作已是維持穩定更新頻率的主流作法——一次集中拍 5~10 支、批次剪輯、以相同設定匯出、一次排程上傳。這種作法的盲點是**每一支單獨看都沒問題，彼此之間卻不一致**：觀眾連看同一系列時會遇到第三集突然要調大音量、第五集畫面明顯偏暗、某一集解析度掉到 720p
- 本工具既有的八類健檢**全都是在衡量單一檔案是否合格**（拿 YouTube 建議位元率、-14 LUFS、CPS 上限等固定門檻去比），沒有任何一項在比較「這批影片彼此之間」；v1.24.0 的分段音量一致性比的也是同一支影片內部各段落，不是跨檔案
- 新增「**系列一致性**」工具（工具列按鈕＋CLI `--seriescheck`）：一次選取同系列的多支影片，比對**整體響度、解析度、畫面更新率、視訊編碼、畫面亮度與色調**是否一致
- **關鍵設計：用相對基準而非絕對門檻**。以整批的**中位數**為基準（用中位數而非平均，整批裡有一支特別離譜時不會把基準拉走），找出偏離整批的那幾支。這樣才分得出「整批都偏暗但彼此一致」（沒問題，是風格）與「其中一支特別暗」（有問題，該修）
- 報告直接指名是哪一支、偏離多少、往哪個方向，並指向本工具既有的對應功能（響度正規化、音訊修復）；容許差異皆可調並自動記憶
- **量測完全重用既有模組**（`audio.measure_loudness`、`videocheck.probe_video_info`、`colorcheck.analyze_color`），不重複實作任何 ffmpeg 呼叫；每個檔案只量測一次，之後所有比較共用同一份結果
- 只報告不自動修：統一整批需要重新編碼整支影片，且「要往哪個基準統一」是創作判斷——與 v1.26.0／v1.28.0／v1.29.0 的保守設計一致

### 修正
- **主視窗工具列在視窗較窄時會把標題與版本號整個擠出畫面、最左邊的按鈕也被裁掉**（功能按鈕累積到六個後，同一列需要 1370px，但最小視窗寬度只有 900px）。改為標題／版本一列、功能按鈕另起一列，按鈕列所需寬度降到 968px；最小視窗寬度同步調整為 980px，確保縮到最小時六個按鈕仍完整可見
- 系列一致性檢查給的檔案不足 2 個時，原本會落到「發生未預期的錯誤，請回報 bug」的泛用訊息；補上專屬的友善錯誤分類，直接說明原因並指出正確作法

## v1.30.0（2026-08-03）

主題：**字幕與語音同步檢查與一鍵校正**——抓出「字幕跟聲音對不上」，並自動算出校正參數。

### 字幕與語音同步檢查
- 調研引用（中英文皆搜）：字幕不同步是匯入既有字幕檔時最常見的翻車點，而且分兩種成因、解法完全不同——**固定偏移**（整軌一致地早或晚，來自匯出設定不同、片頭被剪掉、平台處理延遲）平移即可修好；**逐漸漂移**（開頭對得上、越後面差越多）成因幾乎都是**幀率不符**，為 23.976 fps 來源製作的字幕套到 25 fps 版本會漂移約 4.3%，90 分鐘影片到片尾可差將近 4 秒，單靠平移救不回來。本工具自 v1.19.0 起可匯入既有字幕檔，卻從未檢查過匯入的字幕是否真的對得上這支素材
- 字幕健檢對話框新增「**檢查與語音同步**」：以 ffmpeg 掃出素材的實際語音區間，計算字幕落在語音上的「貼合度」，並搜尋最佳線性校正（縮放倍率 × 偏移秒數），**一次同時涵蓋固定偏移與幀率漂移**
- **縮放倍率只試常見幀率比值**（23.976／24／25／29.97／30 互換）——漂移的實際成因就是幀率不符，限定候選比盲目搜尋任意倍率更穩，也更不容易把本來就對齊的字幕改壞
- **關鍵判準：頭尾貼合度落差**。漂移的物理特徵是誤差隨時間累積，開頭幾乎沒偏、越後面差越多；只看全片平均貼合度會被對得準的開頭稀釋掉而漏判（實測 60 秒素材套上 25→23.976 fps 漂移時，全片平均仍有 0.76 高於門檻，但尾段已掉到 0.66）。因此除了平均值，另外比較頭尾三分之一的落差
- 「**一鍵校正同步**」自動套用建議的縮放與偏移，**只調整時間軸、不改動或刪除任何文字內容**（與 v1.20.0 一鍵延長、v1.23.0 修復重疊的既有慣例一致），逐字動態字幕的時間軸也一併校正
- **安全設計**：只有在校正後貼合度明顯優於原本時才建議套用，避免對本來就對齊的字幕做無謂改動；素材幾乎全程有聲（連續講話或整片配樂）時語音區間提供不了對齊資訊，明確回報「無法可靠判定」而不是硬給一個看似精確的錯誤數字
- 效能：偏移搜尋先粗掃再於最佳點附近細掃，避免整段用細解析度硬跑；語音區間以 bisect 索引查詢
- CLI 新增 `--synccheck`（輸出「檔名_同步檢查.txt」）與 `--syncfix`（直接套用校正另存「檔名_同步校正.ext」，原始字幕檔不受影響），特別適合搭配 `--subs` 匯入的既有字幕檔使用

### 修正
- 字幕健檢報告中殘留的 Markdown 粗體語法（`**...**`）會在純文字報告與 GUI 中原樣顯示星號，改用本專案報告文字既有的「」標示慣例

## v1.29.0（2026-08-02）

主題：**廣告友善度自查（黃標風險預檢）**——上傳前先掃一遍逐字稿，而不是等黃標出現才申訴。

### 廣告友善度自查
- 調研引用（中英文皆搜）：「黃標」（影片被標為不適合多數廣告主）是創作者社群反覆抱怨的痛點——影片剪完上傳後才被標記，申訴要等、重新審查也要等，而問題往往只是某幾句話踩到 YouTube 的「廣告客戶青睞內容規範」。目前社群流傳的自保作法是「先以不公開模式上傳、靜置 12~24 小時等系統掃描」，等於每支影片都要多花一天。本工具已經握有完整逐字稿與時間軸，卻從未用來做這件事
- 字幕健檢對話框的報告新增「**廣告友善度自查**」區塊：掃描目前字幕清單（不論來源），標出可能觸發審查的用詞與其時間點，讓創作者在上傳前幾分鐘內自查
- **刻意避開「逐詞比對」的天真作法**，因為調研有兩個關鍵發現：
  - **YouTube 從未公布官方禁用詞清單**——判定源自規範的主題分類，由 AI 掃描＋人工審查＋廣告主偏好共同決定。因此內建詞表明確定位為「依規範主題整理的自查起點」，報告中也明白標示此限制，不假裝是官方清單
  - **判定看的是「關鍵字叢集」而非單一詞**——業界分析指出，歷史頻道影片出現一次「槍」通常不會被標記，但短時間內密集出現一整串相關詞就會。因此本功能除了列出個別命中，更以**時間窗加權叢集分析**找出「短時間內風險詞密集」的高風險段落，這才是真正該優先處理的地方（實測：零星單次提及不誤報，同樣命中數但時間分散也不誤報）
- 依 YouTube 2025 年 7 月粗俗用語政策更新，**開頭數秒**的用詞影響特別大，本功能對開頭區間（預設 7 秒，可調）的命中另外獨立提醒
- **可依頻道題材裁剪**：新聞、歷史、醫療、遊戲頻道本來就會提及部分詞彙，可填入「排除誤判詞」讓它不再重複標記；也可用「自訂補充詞」加入自己頻道的風險詞。時間窗長度與高風險門檻皆可調並自動記憶
- **只提醒、不自動改動內容**：用詞是否要改屬於創作判斷（同一個詞在教育、新聞、遊戲實況情境下的風險完全不同），自動消音反而可能破壞內容原意——與凍結畫面偵測（v1.26.0）、畫面色偏健檢（v1.28.0）的保守設計一致
- CLI 新增 `--adcheck`（輸出「檔名_廣告友善度.txt」，可與一般轉錄／對齊模式或 `--subs` 併用）

### 修正
- 字幕健檢報告的長「建議：」行折行後，續行會掉回第 0 欄、讀起來像另一個一級項目（`tk.Text` 無懸掛縮排）。加上 `lmargin2` 懸掛縮排修正，既有的字幕品質報告區塊一併受惠

## v1.28.0（2026-08-01）

主題：**畫面曝光與色偏健檢**——抓出「調色沒調好」，上片前先看到而不是等觀眾留言才發現。

### 畫面曝光與色偏健檢
- 調研引用（中英文皆搜）：曝光不足／過曝、白平衡沒調好造成的色偏，是創作者社群反覆提到的剪輯常見錯誤；不少創作者匯出後沒有從頭到尾看過一次就直接上傳，這類問題往往等觀眾留言才發現。本工具既有的影片畫質健檢（v1.17.0）只看位元率／解析度／更新率／編碼等技術規格，完全沒有涵蓋畫面「好不好看」這件事，已與 CHANGELOG 交叉確認不重複
- 「上片前健檢」新增「**畫面曝光與色調**」項目：用 ffmpeg 的 signalstats 濾鏡在全片均勻取樣幾張畫面（免逐幀解碼，速度快），量測平均亮度（偏暗／過曝）與色度平均值（是否偏離中性灰，代表明顯色偏），取樣張數與各項門檻皆可調並自動記憶
- **不自動校色**：色彩校正的「正確答案」因人因場景而異，錯誤的自動校正反而可能讓畫面更難看（例如刻意的暖色調氛圍被誤「修正」成灰白）；比照凍結畫面偵測（v1.26.0）的設計，本功能只負責報告與建議，交由使用者自行判斷是否需要回到剪輯軟體調色
- 已用真實 ffmpeg 合成素材驗證：明顯暖色偏、明顯曝光不足的素材皆正確標出，中性灰的正常素材不會誤判
- CLI 新增 `--colorcheck`（輸出「檔名_色彩健檢.txt」，可與 `--audiocheck`／`--videocheck` 併用）

## v1.27.0（2026-07-31）

主題：**音訊轉視覺化影片**——讓純音訊創作者也能用上整條字幕管線。

### 音訊轉視覺化影片（波形／頻譜）
- 調研引用：Podcast、廣播剪輯、純錄音訪談這類創作者手上常常只有音訊檔，但 YouTube 等平台的慣例是上傳「影片」——沒有畫面的音軌難以被演算法推薦，也不符合大多數觀眾的觀看習慣，創作者因此得另外找線上工具或剪輯軟體，把音訊隨便套一張封面圖生成影片才能上傳，多一道手續。本工具原本也完全無法處理這個需求：燒錄字幕等既有功能都假設來源已經是影片，純音訊檔連「燒錄字幕」都用不了
- 工具列新增「**音訊轉影片**」：把音訊檔轉成附波形或頻譜視覺化的 mp4，可選擇疊在自訂背景圖片（例如節目封面）下緣的一條色帶上，或不指定背景圖時輸出純黑底全畫面視覺化；樣式（波形／頻譜、顏色、解析度）皆可調並自動記憶
- **打通既有管線**：轉出的影片是一支正常的 mp4，可以直接接上本工具既有的轉錄、字幕燒錄、翻譯等整條流程——真實測試已驗證轉出影片可直接餵給「燒錄字幕」成功產生硬字幕影片，等於讓純音訊創作者第一次能用上完整功能
- CLI 新增 `--audiovis`（輸出「檔名_視覺化影片.mp4」，樣式依 config.json 的 audiovis 設定）

## v1.26.0（2026-07-29）

主題：**影片畫質健檢新增凍結畫面偵測**——抓出「聲音仍在播放、畫面卻卡住不動」的錄製瑕疵。

### 凍結畫面偵測
- 調研引用：創作者社群反覆提到剪輯前最怕發現的錄製瑕疵之一，是 SD 卡寫入卡頓、相機緩衝區不足、螢幕錄影軟體當機等造成的「畫面卡住幾秒、聲音卻繼續播放」——這種問題往往上傳後才被觀眾在留言區抓包（「這裡是不是頓住了？」）。本工具既有的影片畫質健檢（v1.17.0）只掃頭尾的靜音／黑畫面，全片中段的凍結畫面完全沒有任何檢查涵蓋
- 上片前健檢的「影片畫質健檢」新增「**凍結畫面**」項目：用 ffmpeg 的 freezedetect 濾鏡掃描全片，列出每一段畫面卡住不動的起訖時間與長度（判定秒數可調，預設 1.0 秒，範圍 0.5～5.0 秒），並提醒檢查來源素材該時間點
- **不自動剪除**：凍結畫面可能是合理的持續鏡頭（教學螢幕錄影中刻意停留的畫面），自動剪除有裁到正常內容的風險，因此比照既有「位元率」「編碼格式」等健檢項目，設計為報告建議、由使用者自行判斷決定是否重新剪輯匯出
- **效能**：凍結畫面偵測與既有的頭尾廢秒偵測（blackdetect／silencedetect）合併進同一次 ffmpeg 全片解碼，不需要多跑一次解碼
- CLI 的 `--videocheck` 報告自動涵蓋凍結畫面檢查（無需新增旗標）

## v1.25.0（2026-07-28）

主題：**Shorts 字幕安全區**——直式短片燒錄字幕自動避開 TikTok／Reels／Shorts 的平台介面遮擋。

### Shorts 字幕安全區（避開平台介面遮擋）
- 調研引用（中英文皆搜）：創作者社群反覆提到「字幕被讚／留言按鈕擋住」「字幕跟帳號名稱文案疊在一起被切掉」是短影音上片後才發現的常見悲劇——2026 年的安全區指南指出 TikTok 底部帳號名稱、文案、配樂資訊與互動按鈕列會固定佔用畫面下緣一大塊區域，頂端也可能有置頂留言或標題覆蓋層；本工具既有的字幕視覺樣式（位置、字級、顏色）是針對橫式（16:9）畫面調整的，Shorts 直式短片輸出（v1.5.0）直接把同一套樣式套用到 9:16 畫布上，完全沒有考慮這些平台專屬的介面保留區
- 上片前健檢視窗的「直式短片」輸出區新增「**字幕安全區**」設定：啟用時（預設開）把字幕垂直位置夾限在安全範圍內（頂端保留 6%、底部保留 22%，皆可調），並縮減左右邊界避開右側常見的互動按鈕欄（保留 5%，可調）；只影響直式短片燒錄的字幕位置，不影響一般橫式字幕樣式本身
- **技術實作**：`subtitle/shorts.py` 新增 `apply_safe_zone()`，依安全範圍夾限樣式的 `position_y`（其餘樣式欄位不變、未超界時原樣沿用同一物件，不做無謂複製）；`subtitle/exporter.py` 的 `cues_to_ass()` 新增可選的 `margin_lr` 參數（省略時維持既有固定 20px 邊界，向後相容所有既有呼叫端）
- 已用真實 ffmpeg 合成素材＋像素驗證確認效果：字幕位置調到平台介面保留區內時，畫面該區域確實出現亮像素（字幕未避開）；啟用安全區後同一位置該區域不再有任何字幕像素（確實避開）

## v1.24.0（2026-07-26）

主題：**分段音量一致性分析與一鍵拉平**——抓出「忽大忽小」的段落，只調整落差過大的部分。

### 分段音量一致性一鍵拉平
- 調研引用（中英文皆搜）：創作者社群反覆提到「整體響度正常，但觀眾還是一路調音量」的困擾——收音距離忽遠忽近、不同場景／不同麥克風剪接在一起、外景風切偶爾蓋過人聲，都會造成段落級的音量落差；本工具既有的響度正規化（v1.5.0）與音訊健檢（v1.11.0）都只看**整體**響度是否偏離固定目標值，沒有任何功能能抓出「某一段特別小聲或特別大聲」這種相對落差
- 上片前健檢對話框新增「**分段音量一致性**」分析：把影片切成固定長度分段（預設 20 秒，可調 10~60 秒），逐段量測實際響度（LUFS），與**整支影片的中位數響度**比較（而非對照 -14 LUFS 這類固定絕對值——不同錄音環境的基準本來就不同），落差超過門檻（預設 3 LU，可調 1.5~8）即標記
- 新增「**拉平音量落差**」：只對被標記的段落套用增益調整（`atrim`＋`volume`＋重新 `concat` 接回），其餘段落與畫面完全不動；畫面軌整段複製（`-c:v copy`），增益調整不改變任何片段時長，因此輸出檔與原始檔逐格對齊、不會有影音不同步風險；沒有偵測到落差過大的段落時按鈕維持停用，不產生無意義的輸出檔
- **技術筆記**：開發過程中發現 ffmpeg `loudnorm` 濾鏡的單次量測（`print_format=json`）搭配 `-ss`／`-t` 時間區間限制會系統性算出錯誤的響度值（以 `volumedetect` 交叉驗證確認），因此分段量測改為「先用 `-ss`／`-t` 擷取該段音訊到暫存 wav，再對完整暫存檔案量測」，避開此限制，已加註在程式碼中避免日後重踩同一個坑
- 無音訊軌的素材（純畫面素材）自動略過此項分析，不影響健檢視窗其餘檢查照常運作
- CLI 新增 `--volumecheck`（輸出「檔名_音量一致性.txt」）與 `--volumefix`（拉平落差過大的段落，輸出「檔名_音量平衡.mp4」，沒有偵測到落差時單檔略過並提示），可與 `--audiocheck`／`--videocheck` 併用

## v1.23.0（2026-07-25）

主題：**字幕健檢新增時間軸重疊檢查與一鍵修復**——避免 YouTube 等平台匯入失敗或播放異常。

### 字幕時間軸重疊檢查與修復
- 調研引用（中英文皆搜）：「字幕時間重疊」與「格式錯誤」是 YouTube 等平台字幕檔案上傳失敗最常見的原因之一；生成字幕（模式一／模式二）內建的正規化流程本就保證輸出不重疊（v1.14.1 修復），但**匯入既有字幕檔（v1.19.0）與手動編輯完全沒有這層保護**——匯入檔案品質不明，手動編輯對話框也從未檢查使用者輸入的起訖時間是否與相鄰句子衝突
- 字幕健檢新增第五類檢查「**字幕重疊**」：掃描目前字幕清單，找出前後時間軸重疊的句子（不論字幕來源，含空白佔位字幕也一併檢查，因為這是結構性問題而非可讀性問題）
- 新增「**一鍵修復重疊**」：截短較早一句的結束時間消除重疊；起始時間相同或反序、單靠截短無法消除時，較早一句改保留極短長度、較晚一句整段順延並保留原本時長不變——邏輯與 v1.14.1 既有的生成流程正規化策略一致，只調整時間軸，不改動或刪除任何文字內容
- CLI 的 `--subcheck` 報告自動涵蓋重疊檢查（無需新增旗標），實際修復需於 GUI 字幕健檢對話框操作

## v1.22.0（2026-07-24）

主題：**重複片段（NG 重錄）偵測與一鍵剪除**——同一句話講了好幾次，剪掉講壞掉的那幾次不用再自己翻逐字稿找。

### 重複片段偵測與剪除
- 調研引用：創作者社群公認「找出並剪掉重講的句子」是初剪另一個吃時間的步驟——坊間工具多半靠語意模型比對逐字稿自動剪去較早的失敗版本，只留最後一次；Premiere 等剪輯軟體原生功能則只給逐字稿，仍要自己讀過、手動刪除
- 直接利用已生成（或匯入）好的字幕 cue：同一時間窗內若兩句文字高度相似，視為「同一句話的前一次嘗試」，標記為候選重複片段，只保留最後一次；相似度門檻、比對時間窗、剪點外擴秒數皆可調並自動記憶
- **內容層級判斷、假陽性風險比純靜音的跳剪高**（例如刻意重複的口號、報數測試麥克風），因此設計為「偵測 → 列出候選逐項勾選確認 → 才真正剪」，而非像跳剪停頓那樣全自動套用；字幕列新增「重複片段偵測」對話框，候選清單預設全選，可自行取消勾選誤判項目
- 剪完影片同步把字幕時間軸重新對齊、被剪掉的句子從字幕清單移除，沿用 v1.21.0 跳剪功能的裁切引擎（trim+concat＋時間軸重新對映）
- CLI 新增 `--retakes`（輸出候選清單「檔名_重複片段.txt」，預設不自動剪）與 `--retakes-cut`（搭配 `--retakes` 使用，剪掉全部候選並輸出「檔名_去重複.mp4」＋對齊後字幕）

## v1.21.0（2026-07-23）

主題：**全片停頓自動跳剪（Jump Cut）**——一次剪掉整支影片的冷場，字幕同步對齊到剪後時間軸。

### 全片停頓自動跳剪
- 調研引用（中英文社群一致）：「剪掉停頓、把初剪跳剪掉」是創作者公認最花時間、最想自動化的步驟——「剪輯師花一半時間在剪停頓」「AI 該處理你討厭的重複工作：剪掉停頓、轉錄、輸出短影音」。本工具已有的「一鍵去頭尾」（v1.17.0）只處理開頭結尾廢秒，口頭禪剪除只去掉特定字詞，中段每一句換氣、思考停頓仍要手剪
- 直接利用已經生成（或匯入）好的字幕 cue 時間軸找出句間過長停頓，一次跳剪整支影片：剪點兩側自動保留緩衝秒數避免斷得突兀，「最短停頓秒數」「緩衝秒數」皆可調並自動記憶；跳剪比例設有安全上限，總跳剪長度超過影片長度的一定比例（預設 60%，可調）時自動中止，避免誤判把整支影片剪爛
- **字幕自動同步對齊**：剪完影片的同時，字幕時間軸也重新對映到新的時間軸——這是坊間跳剪工具很少做到的地方（多半剪完影片、字幕就對不上了）。字幕列新增「自動跳剪停頓」按鈕：可先「預覽跳剪點」（純分析、免呼叫 ffmpeg，瞬間看到能省下多少秒數）再「輸出跳剪版」，完成後字幕清單自動更新為對齊後的新時間軸
- CLI 新增 `--jumpcut`（輸出「檔名_跳剪.mp4」＋時間軸已對齊的字幕「檔名_跳剪.ext」，可與一般轉錄／對齊模式或 `--subs` 併用；`--burn` 燒錄的仍是原始未跳剪的影片，兩者暫不會自動串接）

## v1.20.1（2026-07-23）

主題：**修復驗證發現的問題**——用更多真實網路影片（scikit-video／moviepy 套件內附的樣本影片）擴充測試集後找到的邊界案例。

### 修復
- 音訊一鍵修復（`audiofix.fix_audio`）遇到**完全沒有音軌的影片**（例如純畫面素材、監視器錄影）會直接讓 ffmpeg 噴出英文錯誤訊息中斷（`Stream map '0:a' matches no streams`），而非本工具慣用的友善中文錯誤。修法：執行前先檢查來源是否含音訊串流，沒有時改丟出「此檔案沒有音訊軌，無法進行音訊修復。」

### 測試
- 擴充真實影片測試集：新增數支透過 PyPI 套件（`scikit-video`、`moviepy`）附帶取得的真實影片樣本，涵蓋多種解析度／幀率／編碼／有無音軌組合，用來交叉驗證影片畫質健檢、去頭尾、精彩片段擷取、縮圖候選、品牌套版、Shorts 直式輸出、音訊健檢與本次修復的音訊一鍵修復等既有功能，全數行為正確（僅發現並修復上述一項邊界案例）

## v1.20.0（2026-07-22）

主題：**字幕健檢——閱讀速度（CPS）與行數檢查、一鍵延長過快字幕**。

### 字幕健檢（CPS／顯示時間／行數）
- 調研引用業界字幕 QC 規範：Netflix 成人內容 CPS（每秒字元數）上限 20、BBC 建議 15 以下，CPS 超標是字幕檔案審核最常見的退件原因。本工具生成字幕時雖有斷句規則限制單行字數，但下列情境產生的字幕從未被檢查過：翻譯字幕（雙語內容變兩倍長，時間軸卻沿用原文）、匯入既有字幕檔（v1.19.0，品質不明）、手動新增或編輯的字幕
- 字幕清單新增「**字幕健檢**」按鈕：掃描目前清單（不論字幕來源），標出四類問題——**閱讀速度過快**（CPS 超過門檻，預設 17）、**顯示時間過短**（預設 0.8 秒）、**行數過多**（預設 2 行）、**單行過長**（預設 21 字）；門檻皆可調並自動記憶
- 「**一鍵延長過快字幕**」：把 CPS 超標或顯示過短的句子，在不與下一句重疊的前提下自動延長顯示秒數（只調時間軸、不改文字內容）；下一句銜接太緊、沒有空檔可用時保留原樣，不強行製造新的重疊
- CLI 新增 `--subcheck`（輸出「檔名_字幕健檢.txt」，可與 `--subs`／`--review`／一般轉錄模式併用）

## v1.19.0（2026-07-21）

主題：**匯入既有字幕檔（SRT／VTT）**——手上已經有的字幕檔終於能直接載入本工具，接上編修／樣式／翻譯／燒錄整條既有管線。

### 匯入既有字幕檔（SRT／VTT）
- 調研痛點：本工具目前只能「匯出」字幕，創作者手上已經有的字幕檔——下載的 YouTube 自動字幕、其他工具產出的檔案、舊專案留下的字幕——完全進不了本工具，想修正錯字、換樣式、燒錄硬字幕或加上雙語翻譯，只能開記事本手動改，或另外找網路工具處理一次；TikTok／Reels 等平台又不支援外掛字幕檔，燒錄硬字幕是唯一選項，「匯入既有字幕 → 燒錄」因此是實務上會重複發生的需求
- 字幕列操作區新增「**匯入字幕**」按鈕：選取 .srt／.vtt 檔即可載入，取代目前字幕清單前會先確認（避免誤蓋掉正在編輯的內容），完成後直接接上既有的尋找取代、翻譯字幕、樣式調整、匯出、燒錄流程
- 格式容錯細節：
  - **YouTube VTT 行內標記**：下載下來的 YouTube 自動字幕常帶逐字時間戳 `<00:00:01.500>`、顏色／語者標籤 `<c>` `<v Speaker>`，一律清除只留文字；`<i>` `<b>` `<u>` `<font>` 等常見標記與 `{\an8}` 這類 ASS 覆寫碼也會剝除
  - **編碼自動偵測**：依序嘗試 UTF-8（含 BOM）／繁體 Big5 系列（cp950）／簡體（gb18030），Windows 世界常見的非 UTF-8 字幕檔不再變亂碼
  - **略過壞區塊不中斷**：索引行可省略、時間軸容忍逗號或點分隔的毫秒與缺前導零、CRLF／CR 換行皆可解析；單一區塊格式有誤只略過該段並回報略過數量，不會讓整份檔案匯入失敗
  - VTT 的 NOTE／STYLE／REGION 區塊與 cue settings（position/align 等）正確跳過，時數可省略的 `MM:SS.mmm` 短時間戳也支援
- CLI 新增 `--subs 字幕檔`：搭配一個媒體檔即可跳過語音辨識，直接依自動化輸出設定匯出其他格式並可 `--burn` 燒錄硬字幕（`python main.py --subs 影片.srt --burn 影片.mp4`）
- `subtitle/pipeline.py` 抽出 `export_and_burn()`，讓「轉錄生成字幕」與「匯入既有字幕」兩條路徑共用同一套匯出／燒錄邏輯，行為完全一致

## v1.18.0（2026-07-20）

主題：**字幕翻譯（雙語字幕）**——沿用既有 OpenAI API 一鍵翻譯，免再上傳到第三方工具拼雙語字幕。

### 字幕翻譯（雙語字幕）
- 調研痛點：YouTube 的自動字幕功能無法輸出雙語軌，觀眾端的自動翻譯字幕品質也不穩定——而超過三分之二的 YouTube 觀看時數來自創作者所在地區以外，字幕語言往往是留不留得住海外觀眾的關鍵。目前創作者只能把字幕上傳到另一個第三方翻譯工具，再手動拼成雙語版本
- 生成字幕後按「**翻譯字幕**」：選目標語言（11 種：英/日/韓/繁中/簡中/西/法/德/印尼/越南/泰）與模式即可
  - **雙語模式**：原文與譯文上下兩行顯示，匯出與燒錄皆保留兩行
  - **取代模式**：僅保留譯文，直接取代原字幕內容（可重新生成復原）
- 沿用「轉寫設定」既有的 OpenAI API 金鑰，免再另外申請或填第二把金鑰；批次翻譯（每次數十句）並附格式容錯——單一批次解析失敗時，該筆保留原文，不會整份中斷或漏句
- 逐字動態字幕（卡拉OK／彈出字）在翻譯後自動退回整句顯示：翻譯後的逐字時間軸已對不上新文字，直接拿掉逐字資料，避免顯示錯亂
- 誠實提醒：譯文由 AI 產生，建議翻譯後抽查幾句，尤其是專有名詞與語氣

## v1.17.1（2026-07-19）

主題：**真實影片實測後的修正**——以網路取得的真實素材（Big Buck Bunny、實拍街景等）對全功能實測，修正兩個實測才暴露的問題。

### 廢秒偵測：橋接短暫聲響
- 真實素材的開頭廢秒中常夾著一瞬間的聲響（相機提示音、椅子聲）——實測發現 0.1 秒的斷點會讓 2.2 秒的開頭廢秒被低估成 1.07 秒，修剪建議跟著失準
- 修法：兩段靜音（或黑畫面）之間的空隙小於 0.35 秒時視為同一段廢秒；空隙夠長則維持分開判定，不會誤併真正的內容

### 建議位元率顯示修正
- 低解析度素材的建議值 2.5 Mbps 先前被顯示成「2 Mbps」（四捨五入到整數），判定本身正確但報告數字誤導
- 修法：改用自然格式顯示（2.5 顯示 2.5、8 顯示 8）

### 真實素材實測紀錄（本次一併完成）
- 健檢：BBB 真實音軌量出 -33.3 LUFS 太小聲、720p@1.6Mbps 位元率正確標紅；無聲影片優雅降級；低解析素材正確提醒
- 品牌套版：640x272 實拍片頭＋720p 主片異質接續正常、浮水印影格目視正確
- Shorts 裁切／模糊、封面候選、音訊修復（-33.3→約 -13 LUFS）、配樂閃避自動靈敏度（真實小聲音軌 → 門檻自動降至 0.0054）皆以真實影片通過

## v1.17.0（2026-07-19）

主題：**上片前影片畫質健檢、一鍵去頭尾**——畫質變糊不要上傳後才發現，開頭廢秒不用再手剪。

### 影片畫質健檢（併入健檢視窗）
- 調研顯示「上傳後畫質變糊」最常見的原因是輸出位元率不足：YouTube 對每支影片都會再壓縮一次，來源位元率低於建議值時丟失的細節救不回來。健檢視窗現在連畫質一起檢查（免轉錄、純 ffprobe／ffmpeg，純音訊檔自動略過）：
  - **位元率**對照 YouTube 官方建議值（依解析度與更新率：1080p30→8 Mbps、1080p60→12、4K→35 起），不足直接標紅；寬嚴倍率可調並記憶
  - **解析度**（未達 720p 提醒；直式標註適合 Shorts）、**畫面更新率**（非標準值提醒可變更新率風險）、**編碼格式**（非主流編碼建議重新輸出 H.264）
- CLI 新增 `--videocheck`（輸出「素材_影片健檢.txt」，可與 `--audiocheck` 併用）

### 一鍵去頭尾（廢秒偵測修剪）
- 開頭的開機、清喉嚨廢秒是留存率殺手，結尾長時間空白拉低平均觀看時長——但逐支手剪很繁瑣。健檢時以 silencedetect＋blackdetect 掃描頭尾的無聲／黑畫面長度，超過門檻（可調）即提醒並**自動帶入修剪建議秒數**（留 0.25 秒緩衝，避免切得太貼）
- 微調秒數後按「**輸出修剪版**」輸出 `素材_修剪.mp4`：重新編碼確保剪點精準（串流複製只能切在關鍵格），原始檔不受影響；修剪後不足 1 秒會擋下防呆
- 健檢視窗更名為「上片前健檢」，並加高容納新區塊

## v1.16.0（2026-07-18）

主題：**既有功能精修**——深色主題覆蓋所有對話框、封面候選短素材湊得滿、孤字合併不再拉長字幕。

### 深色主題全面化（所有對話框）
- v1.14.1 修好了主視窗的深色主題，但審片助手、音訊健檢、品牌套版、尋找取代、錯誤視窗、ffmpeg 安裝等對話框仍殘留 classic tk 元件，深色模式下會出現大小不一的亮色色塊
- 全部改用 ttk 主題化元件；「尚未安裝 ffmpeg」等警示橫幅刻意保留固定的黃底配色（兩種主題下都是明確的警告視覺）
- 審片助手預設高度 850→900，修正底部「廣告插入點」列在預設大小下被裁到一半的問題
- 深、淺色主題皆以真實截圖逐窗確認

### 封面候選：短素材自動縮小取樣間隔
- 候選間最小間隔（預設 8 秒）在短素材上會讓張數湊不滿（20 秒素材要 6 張時，實際只出得來 2 張）——間隔現在會依素材長度自動縮小（長素材不受影響），並修正均勻取樣的浮點誤差誤剔除
- 短素材上取樣窗口高度重疊時，新增全體候補回填：某窗口選不出符合間隔的格時，改從所有取樣格中按清晰度高→低補滿張數

### 孤字合併：加上時間防護
- 斷句時 2 字以下的「孤字碎片」會併回前一句，但舊版不看時間差：停頓後的獨立反應詞（「嗯」「喔」）也被併回，導致前一句字幕的顯示時間被拉長、橫跨整段靜音
- 現在間隔超過 1 秒即視為獨立語句不併回；併回時改用 CJK 感知組字（英文孤字如「ok」前會正確補空白，不再黏字）

## v1.15.0（2026-07-17）

主題：**發佈包標題真標題化、配樂閃避自動適應人聲音量**——修補前次全功能驗證找到的兩個「能動但不夠好用」的缺口。

### 建議標題不再是逐字稿截斷
- 舊版的「建議標題」只是把精彩段落文字硬截前 40 字，常常是半句話（甚至切在英文單字中間），實用價值低，等於還是要自己想標題
- 改為按有效標題的常見型態各出一手，直接可用或稍加組合：
  - **精華句**：精彩分數最高的段落，剝掉「然後、就是、那個」等句首贅詞，以**完整子句**湊到長度上限，不再攔腰切斷
  - **疑問句**：素材中實際講出的疑問（句尾「嗎／呢」或句首「為什麼／怎麼」），自動補上問號——好奇缺口式標題，觀眾想知道答案
  - **數字句**：含具體數字的高分句（「只要三百塊」比「很便宜」有力）
  - **關鍵字冠頭**：【關鍵字】＋精華子句，關鍵字取自訂情緒詞（頻道口號、產品名優先）或素材高頻詞
- 候選彼此去重（忽略問號差異）；仍為純文字啟發式、不需連網或 AI 模型；數量與長度上限沿用既有 `publish` 設定

### 配樂閃避自動適應人聲音量
- 舊版閃避靈敏度是**絕對音量門檻**（預設 0.06）：人聲偏小聲（還沒做響度正規化）的素材可能整段都低於門檻，**閃避完全不觸發**——前次全功能驗證中實測重現過這個情境
- 新增「**自動適應人聲音量**」（預設開啟）：混音前先量測影片人聲的整體響度（重用既有 loudnorm 量測），把觸發門檻自動設在人聲響度再低 12 dB 處——講話段落必然高於門檻、必然觸發閃避，小聲人聲也適用
- 量測失敗（無人聲、檔案異常）時自動退回手動靈敏度值，不中斷混音；取消勾選即回到全手動模式（手動旋鈕在自動模式下反灰，避免混淆）
- 設定自動記憶於 `config.json`（`ducking.auto_sensitivity`），舊設定檔自動取得預設值

## v1.14.1（2026-07-16）

主題：**修復四項驗證中發現的實際缺陷**——英文字幕黏字、深色主題主視窗大面積失效、對齊模式字幕重疊、視窗預設寬度裁切樣式面板。

### 英文（拉丁字）字幕黏字
- 模式一（音訊轉錄）辨識英文或中英混排內容時，逐字組句直接用字串相加，完全不補空白，導致字幕變成「Thisisatestoflatinsegmentationlogicheretoday.」這種無法閱讀的黏字——轉寫模組本就會把 Whisper 逐字 token 的前導空白去掉（供其他去重/審片邏輯使用），組句時卻沒有把空白補回去
- 修法：改用與審片模組（`subtitle/review.py`）相同的中日韓（CJK）感知組句邏輯——拉丁字與拉丁字之間補空白、CJK 字元前後不補空白，兩處合併為 `subtitle/segmenter.py` 的共用函式 `join_words()`，避免邏輯各自維護一份；同時修正標點前誤補孤立空白的問題（如「today .」），套用範圍涵蓋一般字幕組句與逐字動態字幕（卡拉OK 模式）的字詞重組
- 中文與英文混排（如「我們用OBS Studio錄影」）僅在拉丁-拉丁交界補空白，中文前後維持原樣不變

### 深色主題主視窗大面積失效
- 實際截圖比對發現：切換「深色」主題時，只有工具列少數按鈕變色，主視窗其餘大部分（模式選擇、檔案欄位、轉寫/斷句設定、匯出按鈕等）維持淺色——因為這些元件過去使用的是不受 sv_ttk 主題引擎管理的 classic tk 元件（`tk.Frame`／`tk.Label`／`tk.Entry`／`tk.Button`／`tk.Radiobutton`），而非 ttk 版本
- 修法：主視窗可設定元件全面改用對應的 ttk 版本；可捲動容器（`gui/scrollable.py`）的 `Canvas` 本身不是 ttk 元件、不會自動變色，改為依目前主題設定背景色，並在主題切換時即時同步（含執行期切換，不必重開視窗）
- 已以真實 Xvfb 螢幕截圖驗證淺色／深色兩種主題、以及執行期即時切換

### 對齊模式（模式二）字幕重疊
- 兩句字幕起始時間非常接近（甚至完全相同）時，先套用「最短秒數」把前一句延長，再修正重疊時卻只靠「截短前一句」，起始時間相同或反序的情況下無法真正消除重疊，導致輸出的 SRT 出現前一句還沒結束、下一句就已經開始的重疊時間軸
- 修法：新增最終正規化流程，確保輸出嚴格不重疊、時間單調遞增——不重疊優先於最短秒數：能截短前一句就截短；連截短都無法避免重疊時（起始時間相同或反序），前一句維持極短長度、下一句整段順延，保留下一句原本的時長不變

### 視窗預設寬度裁切樣式面板
- 預設視窗大小 1180×780 在右側樣式面板會裁掉「選擇顏色」按鈕，需要手動拉寬或拖動水平捲軸才看得到——已以真實截圖確認
- 修法：預設視窗大小調整為 1400×800，並放寬最小可縮視窗尺寸；1400 寬度下右側面板所有按鈕與控制項皆完整可見，不需捲動

## v1.14.0（2026-07-15）

主題：**品牌套版一鍵套用**——片頭片尾不用再每支影片手動接一次，浮水印位置也不用重新調。

### 片頭／片尾一鍵接續
- 新增「**品牌套版**」對話框（工具列開啟）：設定一次片頭、片尾影片路徑，之後每支影片按「輸出套版影片」即可自動接續——調研顯示創作者最常抱怨的重複工作之一就是每支影片都要重新做一次同樣的接片頭片尾
- 片頭／片尾與來源影片尺寸、畫面更新率、音訊格式不一致時自動正規化，沒有音軌的片頭／片尾（常見的純動畫 bumper）自動補靜音音軌，避免無法接續
- 只設定其中一項（只有片頭或只有片尾）也可正常使用；皆可與浮水印同時套用

### 浮水印（Logo）自動疊加
- 同一個對話框可設定浮水印圖片：位置（左上／右上／左下／右下／置中）、大小（相對畫面寬度 5~50%）、透明度（10~100%）、邊緣留白皆可調並自動記憶
- 全片皆疊加；只用浮水印（不接片頭片尾）時保持音軌直接複製，不多做無謂的重新編碼
- CLI 新增 `--branding`（免開視窗，套用 config.json 已設定的內容）

### 開發品質
- 修復 `_stream_progress` 進度訊息固定顯示「燒錄中／字幕燒錄完成」的問題（不論實際在跑哪個功能都顯示這兩句，例如音訊修復時也顯示「燒錄中」）：改為依呼叫端傳入正確的動作名稱顯示
- 友善錯誤翻譯的「找不到檔案」規則補上片頭／片尾／浮水印找不到時的專屬比對，確保 CLI 批次模式（錯誤先轉成純文字字串再分類）也能正確歸類，不會落入「未知錯誤」

## v1.13.1（2026-07-15）

主題：**修復 Issue #28**——勾選框顯示異常、外部 Whisper 偶發解析失敗。

### 勾選框顯示 bug
- 深色／淺色主題（sv_ttk）套用時，全部 classic `tk.Checkbutton`（音訊修復、審片偵測設定、自動化輸出格式等所有勾選框）的勾選指示器不會跟著主題調色盤走：按下時短暫可見打勾，放開滑鼠後又「看起來」變回空白——實際狀態未必真的跑掉，但使用者完全無法信任畫面
- 修法：全面改用 `ttk.Checkbutton`（與既有已知正常的「尋找取代」對話框做法一致），確保勾選狀態在任何主題下都正確顯示

### 外部 Whisper 輸出解析失敗
- 打包版使用外部 Python 執行 Whisper 時，若該 Python 環境在轉錄過程印出任何額外文字（模型下載進度、numba／torch 警告等，即使並非 Python `print` 而是 C 層級輸出），都可能混進 stdout，導致本程式讀不出預期的 JSON、跳出「無法解析外部 Whisper 的輸出結果」
- 修法：以 `os.dup2` 把 stdout 檔案描述元在推論期間暫時導到 devnull，涵蓋 Python 與 C 層級輸出，推論結束才切回並寫入最終 JSON，確保交回本程式的內容只有乾淨的一份結果
- 錯誤訊息系統同步補上此情境的友善翻譯（原因＋解法），即使仍有極端情況觸發也不會再落入「未知錯誤」

## v1.13.0（2026-07-15）

主題：**音訊一鍵修復、mid-roll 廣告插入點建議**——底噪不用再上傳外部網站處理，廣告點不用再拉一次時間軸。

### 音訊一鍵修復（健檢視窗）
- 健檢視窗下方新增「**音訊修復**」區：勾選項目一鍵輸出 `素材_修復.mp4`（畫面原樣複製、僅處理音軌；純音訊檔輸出 `.m4a`）——調研顯示創作者為了去背噪常得把整支影片上傳到線上 AI 工具，現在程式內直接修
  - **降噪**：FFT 頻譜降噪（afftdn），對冷氣聲、電流聲、嘶聲等穩態底噪有效；強度可調（6~40 dB，預設 12，過高人聲會發悶）
  - **去低頻隆隆**：高通濾波切掉桌面震動、風切、空調低頻（40~200 Hz，預設 80，不傷人聲）
  - **響度正規化**：重用既有兩階段 loudnorm，同步校正到 YouTube 標準
- 健檢報告的「底噪偏高」「太小聲」建議直接指向此功能，診斷→修復一站完成；修完可再跑健檢比對
- 勾選與強度自動記憶；CLI 新增 `--audiofix`（免轉錄批次修復）

### mid-roll 廣告插入點建議（審片助手）
- 輸出區新增「**複製廣告插入點**」：從審片分析的停頓資料挑出 mid-roll 建議時間點——YouTube 官方指出放在**自然停頓、話題轉換處**的廣告點留存較高、較容易被投放，插在句中的常被系統跳過
- 只挑講話段落間 ≥1.2 秒的自然停頓，停頓越久越優先；最小間隔（預設 4 分鐘）與數量上限（預設 6）可調並記憶；自動避開頭 2 分鐘與尾 1 分鐘；不足 8 分鐘的影片明確提示不適用
- 複製結果含時間戳、停頓秒數與前後句摘要，不開影片即可確認位置；發佈包自動附上同一份建議（GUI 與 CLI `--review` 皆是）

## v1.12.0（2026-07-14）

主題：**錯誤訊息看得懂、ffmpeg 一鍵裝**——回應 Issue #24。

### 友善錯誤訊息（原因＋解決方法）
- 全新錯誤視窗取代原本的一行技術訊息：每個錯誤都分「**原因**」（白話說明為什麼）與「**解決方法**」（具體要做什麼）兩段呈現；原始技術訊息（例外內容、ffmpeg stderr）收進可展開的「技術細節」，一鍵複製即可回報
- 內建常見情境對照：缺 ffmpeg、缺 Whisper／外部 Python 未設定、API 金鑰未填、無人聲、檔案被移動、輸出位置無權限／被占用、磁碟空間不足、ffmpeg 處理中斷等，各附對應解法；無法歸類的錯誤引導附細節與 `app.log` 回報
- 審片助手、音訊健檢、配樂助手、主流程（生成／燒錄）與 CLI 批次全部套用同一套翻譯；CLI 失敗摘要同樣顯示原因＋解法
- 新增錯誤記錄檔 `app.log`（程式資料夾，自動輪替），回報問題時可直接附上

### ffmpeg 一鍵自動安裝
- 「請先安裝 ffmpeg 並加入 PATH」的門檻正式移除：審片助手與音訊健檢視窗偵測不到 ffmpeg 時直接顯示提示條＋「**自動安裝 ffmpeg**」按鈕，錯誤視窗也提供同一按鈕
- 自動下載官方靜態建置（約 80~100 MB，只需一次）並安裝到程式自己的資料夾——**免管理員權限、不改動系統 PATH**，裝完立即可用、下次啟動自動生效
- 程式資料夾不可寫（如 Program Files）時自動改裝到使用者資料夾；下載失敗（離線、防火牆）會給出手動安裝的明確路徑（把 ffmpeg.exe/ffprobe.exe 放進 `tools\ffmpeg\bin\` 即可，同樣免 PATH）

## v1.11.0（2026-07-13）

主題：**封面候選一鍵擷取、上片前音訊健檢**——縮圖素材不再拖時間軸截圖，聲音出包在上傳前就抓到。

### 封面候選擷取（審片助手）
- 輸出區新增「**擷取封面候選圖**」：從**精彩分數最高的段落**取樣畫面，以 ffmpeg 邊緣能量評分（sobel＋signalstats，動態模糊與黑畫面都得低分）自動挑出最清晰的格，輸出 `素材_封面01.png`…（1280 寬 YouTube 封面標準、依清晰度排序）——不必再來回拖時間軸肉眼找一格能用的畫面
- 可調參數（自動記憶）：張數（2~12，預設 6）、候選間最小間隔（預設 8 秒，避免全擠同一幕）、優先取精彩段落（可關閉）
- 評分先把畫面縮到 320 寬再計算，10 分鐘素材預設僅需解碼約 72 秒畫面，速度快、資源省
- CLI：`--thumbnails` 單獨使用（免轉錄、整片均勻取樣）或與 `--review` 併用（優先精彩段落）

### 音訊健檢（上片前檢查聲音）
- 工具列新增「**音訊健檢（上片前）**」：一鍵掃描音軌（不改動原始檔），檢查表回報四項——**整體響度**（太小聲 YouTube 不會幫你調大）、**爆音**（峰值貼頂＋波形削平）、**底噪**（安靜段落的嘶聲）、**聲道平衡**（單邊無聲、左右不均）——每項附具體修法建議
- 爆音、太小聲、單邊聲道都是上傳後才被留言提醒的常見悲劇；健檢以 ffmpeg astats＋loudnorm 量測，免轉錄、速度快
- 四項判定門檻皆可調並自動記憶；報告可複製或另存 `素材_音訊健檢.txt`
- CLI：`--audiocheck` 免開視窗批次健檢，可與 `--review` 併用

## v1.10.0（2026-07-13）

主題：**口頭禪自動剪除、一鍵發佈包**——連「呃、嗯」都自動剪掉，剪完片連標題標籤都幫你想好。

### 口頭禪單字自動剪除（Descript 式 filler-word removal）
- 審片助手輸出區新增「**剪除口頭禪字**」勾選：粗剪影片、精彩合輯與 EDL 輸出時，把保留段落裡的「呃、嗯、欸」等口頭禪**字詞本身**從時間軸挖掉——之前只能標記整段供複查，現在直接逐字剪除
- 內建防抖動保護：太短的口頭禪（<0.12 秒）不剪、剪點向內縮避免切到相鄰字、剪除後過短的殘片自動捨棄
- 口頭禪字表沿用「偵測設定」的自訂字表；相鄰口頭禪（呃⋯嗯連著講）自動合併成一刀
- 設定自動記憶；EDL 與粗剪使用同一份剪點，進剪輯軟體精修也一致

### 一鍵發佈包（標題／描述／標籤）
- 審片助手輸出區新增「**匯出發佈包**」：一鍵輸出 `素材_發佈包.txt`，內含：
  - **建議標題候選**（預設 3 個）：取精彩分數最高的段落文字，截到行動裝置可見的 40 字內
  - **描述草稿**：開場鉤子（第一個保留段落）＋建議章節＋hashtag 行，複製後自行潤飾
  - **建議標籤**（預設 15 個）：素材中實際講到的高頻詞——自訂情緒詞（頻道口號、產品名）優先、中文高頻詞組（出現 ≥3 次、剔除虛詞）、英文高頻詞
- CLI 批次審片（`--review`）逐檔自動附發佈包
- 數量與長度可調（`config.json` 的 `publish`：標題候選數 2~6、標題長度 20~60、標籤數 5~30）

### 開發品質
- 修復發佈流程偶發失敗：Release 防重複守衛改帶認證查詢並容錯（先前匿名查詢在 CI 共享 IP 上可能踩到 API 限額 403，導致整個建置中斷）

## v1.9.0（2026-07-12）

主題：**配樂助手——背景音樂自動閃避（Audio Ducking）**。

- 新增工具列「**配樂助手（自動閃避）**」：選定影片與背景音樂後一鍵輸出混音影片，講話時自動壓低音樂音量、安靜時自動恢復——不必再逐段拉關鍵影格手動調音量（Premiere、Filmora、DaVinci Resolve 2026 年皆已內建同類功能）
- 三個可調強度旋鈕（自動記憶）：
  - **音樂音量**（0.05～1.0，預設 0.35）：混音前的音樂基礎音量
  - **閃避強度**（1～20，預設 8）：講話時音樂被壓低的程度，越高壓得越低
  - **閃避靈敏度**（0.01～0.5，預設 0.06）：數值越低，越輕的講話聲就會觸發閃避
- 背景音樂比影片短時自動循環播放，輸出自動裁到影片長度；影像串流原樣複製、僅音軌重新混音，畫質不受影響
- 核心邏輯獨立於 `subtitle/audio.py`（`mix_background_music`），以 ffmpeg 的 `sidechaincompress` 濾鏡實作，不依賴任何額外的機器學習模型或即時處理

## v1.8.0（2026-07-12）

主題：**自動修正詞庫、人聲頻帶音量分析**——錯字修一次就記住、精彩偵測不被配樂帶偏。

### 自動修正詞庫（校對再提速）
- 「尋找取代」對話框新增「**自動修正詞庫**」：按「把目前取代存為規則」，該錯字之後**每次生成字幕都自動修正**——語音辨識的慣性錯字（人名、產品名、同音字）只需修一次
- 一鍵完成、CLI 批次與手動生成全部套用；規則可隨時在對話框檢視與刪除，同一錯字重存會更新規則
- 規則存於 `config.json` 的 `corrections`，與逐字動態字幕相容（可同步的取代會同步到逐字資料）

### 人聲頻帶音量分析（精彩偵測更準）
- 審片的音量能量分析預設**聚焦人聲頻帶（150～4000 Hz）**：背景配樂的低頻鼓點與高頻效果音不再拉高音量訊號，精彩偵測更貼近「講話的起伏」——回應各家工具在背景音樂下準度下降的通病
- 「偵測設定」新增「**音量聚焦人聲頻帶**」開關（預設開）；素材以音樂為主體（如樂器演奏頻道）時可關閉
- RMS 計算改用 C 實作（audioop）加速，長素材的音量分析更快；環境不支援時自動退回原演算法

## v1.7.0（2026-07-12）

主題：**校對提速與動態字幕**——尋找取代、逐字動態字幕、精彩訊號權重、多檔審片彙總。

### 字幕尋找與取代（校對提速）
- 字幕清單新增「**尋找取代**」：同一個錯字（人名、產品名、同音字）整集重複出現時，一次全部修正，不必逐句雙擊
- 「找下一個」循環跳到命中句、「取代目前」逐句確認、「全部取代」一鍵修完並回報次數；可勾選區分大小寫（英文）
- 對話框開著仍可操作主視窗清單，即時顯示「幾句字幕、幾處符合」

### 逐字動態字幕（卡拉OK／單字彈出）
- 「字幕視覺調整」新增「**動態字幕**」模式，燒錄影片與 ASS 匯出時生效：
  - **逐字上色（卡拉OK）**：整句顯示，講到哪個字換色（顏色沿用重點字顏色）
  - **單字彈出**：只顯示當前字詞並帶放大彈出動畫——Shorts／TikTok 常見版式
- 適用一鍵完成、手動燒錄與審片助手的直式短片；需要逐字時間軸（模式一音訊轉錄），手動或文字稿對齊字幕自動退回整句
- 尋找取代會同步更新逐字資料；無法完整同步（跨字詞取代）時該句自動退回整句，確保不殘留舊字

### 精彩偵測訊號權重（自動化與創作彈性平衡）
- 「偵測設定」新增「**精彩訊號權重**」：音量、語速、情緒詞、驚嘆句四種訊號各自可調 0～3 倍（0＝完全停用該訊號）
- 例：教學型頻道可把情緒詞權重調低、音量調高；遊戲實況反之——不再只有單一敏感度倍率

### 多檔審片彙總（跨檔精彩 Top N）
- CLI 批次審片（`--review` 多檔）自動另輸出「**審片彙總.html**」與「**審片彙總_精彩TopN.csv**」：整批素材的精彩片段跨檔依分數排序，一眼看出「這批素材裡最值得用的片段」
- 各素材統計（總長、保留、精彩段數、冷場、口頭禪）一頁對照；Top N 段數可調（`batch_top_n`，3～50，預設 10）

## v1.6.1（2026-07-12）

主題：**體驗精修**。

- **重點字上色即時預覽**：「即時字幕預覽」現在會直接顯示重點字的顏色效果（單行時逐段分色渲染，含描邊；多行或超寬時維持單色避免排版錯位），調整詞彙與顏色立即可見，不必先燒錄影片
- **HTML 審片報告新增「建議章節」區塊**：套用章節智慧合併結果，可直接複製貼到 YouTube 說明欄（GUI 與 CLI `--review` 皆附）
- **審片助手輸出位置尊重「自動化輸出→輸出資料夾」設定**：粗剪、精彩合輯、直式短片、報告等所有審片輸出與主流程一致（留空＝來源資料夾）
- 審片助手視窗預設尺寸調整（1020x760），容納偵測設定與三排輸出按鈕不擁擠

## v1.6.0（2026-07-12）

主題：**YouTube 章節智慧合併、重點字上色（CapCut 風格）**。本版一併包含尚未發佈的 v1.5.0 全部功能。

### YouTube 章節智慧合併
- 章節不再一段一章（創作者常見抱怨「章節分太細」）：只在**大停頓（剪掉的冷場處）且目前章節已達最短長度**時才切新章，標題取該章第一段開頭
- 「偵測設定」新增**章節最短長度**（10～600 秒，預設 60），依影片節奏自行調整
- 首章固定 0:00（YouTube 規定），複製即貼說明欄

### 重點字上色
- 「字幕視覺調整」新增**重點字上色**：勾選後，指定詞彙（逗號或空白分隔）在**燒錄影片與 ASS 匯出**時自動換色強調（預設金黃，可選色）——CapCut 風格的重點標記，免逐句手動上色
- 適用於一鍵完成燒錄、手動燒錄、精彩合輯與直式短片；SRT／VTT／TXT 純文字輸出不受影響
- 詞彙比對拉丁字不分大小寫、長詞優先避免部分遮蔽

## v1.5.0（2026-07-10）

主題：**Shorts 直式短片一鍵輸出、響度正規化（YouTube -14 LUFS）**。

### Shorts 直式短片輸出（審片助手）
- 在段落清單**選取任意段落（可多選）→「輸出直式短片」**，每段各輸出一支 9:16（1080x1920）直式影片；未選取時自動輸出所有精彩片段
- 兩種版式：
  - **裁切**：以可調的水平焦點（0 最左～1 最右）裁出直式畫面，適合人物固定在某側
  - **模糊背景**：完整畫面置中、上下以放大模糊的原畫面填滿，不損失內容
- 可勾選**燒錄字幕**：沿用字幕視覺樣式，該段字幕由逐字時間軸重建、時間自動平移
- 可勾選**響度正規化**；版式與焦點設定自動記憶
- 輸出自動命名（`素材_短片01.mp4`…），逐支顯示進度

### 響度正規化（loudnorm）
- YouTube 會把超過 -14 LUFS 的音訊調小，但**太小聲的不會調大**——音量偏低的影片需自行正規化
- 「自動化輸出」新增**「燒錄時響度正規化」**與目標值（-30～-8 LUFS，預設 -14＝YouTube 標準）：兩階段處理（先量測、再線性校正），音質與精準度優於單次動態調整
- 手動「燒錄字幕到影片」與一鍵完成、CLI（`--loudnorm`／`--no-loudnorm`）皆支援

### 開發品質
- 功能測試正式納入版控（`tests/`，共 110 項）：`python tests/run_all.py` 一鍵執行，
  以替身隔離 whisper／ffmpeg 相依，無須外部程式即可驗證核心邏輯

## v1.4.0（2026-07-09）

主題：**修復重複字幕、審片偵測全面可調、轉錄快取省時省資源**。

### 修復
- **修復 Issue #4「重複跑字幕」**：Whisper 遇到靜音、背景音樂或雜訊時會重複輸出同一句話（looping hallucination），造成同一時間點出現多句相同字幕。加入三層防護：
  - 字詞層：相同文字且時間戳重疊的字只保留一個
  - 迴圈層：同一組字連續重複（單字 ≥4 次、片語 ≥3 次）自動收斂為一組
  - 字幕層：相鄰且內容相同、時間重疊的字幕合併為一句

### 審片偵測參數全面可調（審片助手「偵測設定」區）
- **精彩敏感度**（0.2～3.0 倍）：>1 更容易標記精彩、<1 更嚴格
- **自訂情緒詞**：逗號或空白分隔，附加於內建詞庫（例如自己頻道的口號、梗詞）
- **口頭禪字表**：自訂要偵測的填充字（預設「呃嗯欸蛤齁」）
- **冷場門檻、段落切分停頓、重複判定相似度**皆可調
- 所有參數自動記憶於 config.json，GUI 與 CLI `--review` 共用

### 效能：轉錄結果快取
- 語音辨識是最耗時的步驟；同一檔案在「一鍵完成 → 審片助手」「調整斷句後重新生成」等情境會重複轉錄。現在同一檔案（大小與修改時間一致）且轉寫設定相同時自動重用結果，**免重跑整段辨識**
- 快取存於程式資料夾 `transcribe_cache/`，自動限量 40 筆（刪最舊）；「轉寫設定」可勾選停用

## v1.3.0（隨 v1.4.0 一併發佈）

主題：**減少影片後製的繁雜瑣碎步驟**——一鍵完成、批次處理與審片助手。

### 一鍵完成與批次自動化
- 新增「**一鍵完成（生成＋匯出＋燒錄）**」按鈕：整條流程一個動作完成，全程不跳存檔對話框
- 新增「**自動化輸出**」設定區：勾選匯出格式（SRT/VTT/ASS/TXT）、是否燒錄、輸出資料夾，設定記憶於 `config.json`
- 檔案選擇支援**多選批次**：逐檔顯示進度，單檔失敗不中斷整批，結束後列出成功／失敗清單
- 輸出檔自動以來源檔名命名（`影片.mp4` → `影片.srt`、`影片_subtitled.mp4`），同名自動加 `(1)` 流水號不覆蓋
- 模式二批次時自動讀取媒體檔旁同名 `.txt` 文字稿（sidecar）
- 新增**命令列批次模式**：`python main.py 影片1.mp4 影片2.mp4`，旗標 `--mode` / `--formats` / `--burn` / `--no-burn` / `--output-dir` 僅影響本次執行
- **拖檔到 exe 圖示即處理**：依記憶設定靜默跑完，結束以視窗回報結果

### 審片助手（工具列「審片助手（找片段）」）
- **文字審片**：轉錄素材切成講話段落，讀文字取代來回拉時間軸
- 自動標記四類內容並建議取捨：
  - **精彩片段**：綜合音量能量（ffmpeg PCM RMS）、語速、情緒詞、驚嘆句評分（無 ffmpeg 時退化為純文字訊號）
  - **冷場**（含片尾，門檻可調）→ 預設捨棄
  - **重複拍攝**（同句重講相似度比對）→ 只保留最後一次
  - **口頭禪密集**（呃、嗯、欸、um…）→ 標記供複查
- **彩色時間軸**：綠＝精彩、琥珀＝待審視、灰＝冷場、藍＝一般，捨棄段網點淡化；點色塊跳到段落；清單列同色系上底色
- 「只看精彩／只看待審視」篩選、關鍵字跳查、雙擊／空白鍵切換保留、保留／精彩／冷場時長統計
- 七種輸出（皆自動命名）：**粗剪影片**（自動跳剪）、**精彩合輯**（Shorts 素材）、**HTML 審片報告**（單檔含時間軸＋統計＋段落表）、**EDL（CMX3600）**、**審片標記字幕 SRT**、**CSV 清單**、**YouTube 章節**
- CLI 新增 `--review` 批次審片：逐檔輸出審片清單 CSV 與 HTML 報告

## v1.2.0
- 美化排版色彩，新增 dark / light 主題切換（sv-ttk）

## v1.1.x
- 各處理步驟新增進度條
- 演算法優化、擴充輸出格式（VTT/ASS/TXT）、影片字幕燒錄（hardsub）、手動字幕模式
- 內建自動更新（啟動時向 GitHub Releases 檢查新版）

## v1.0.0
- 首個版本：音訊轉錄與文字稿對齊兩種模式、智慧斷句、即時預覽、字幕樣式調整、SRT 匯出、設定記憶
